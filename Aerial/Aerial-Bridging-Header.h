//
//  Use this file to import your target's public headers that you would like to expose to Swift.
//

// All Objective-C classes have been replaced with Swift implementations.
// The one remaining ObjC source is the NSException catcher shared with
// the wallpaper extension (Swift cannot catch NSException); it compiles
// into this target so AerialTests can exercise it.
#import "../WallpaperExtension/Safety/ObjCExceptionCatcher.h"
